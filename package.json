{
  "name": "shift-alarm-push-server",
  "version": "1.0.0",
  "description": "Backup web-push server for shift_alarm_v3.html — fires shift alarms via push even when the app is fully closed.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "web-push": "^3.6.7"
  }
}
